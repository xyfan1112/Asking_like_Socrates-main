{
  "schema_version": "vieo_deployment_v1",
  "base_version": "1.2.3-r3",
  "package_profile": "vieo_temporary_machine",
  "workspace_root": "/home/vieo/vieo/fxy_workspace",
  "project_root": "/home/vieo/vieo/fxy_workspace/Asking_like_Socrates-main",
  "fxy_root": "/home/vieo/vieo/fxy_workspace/fxy",
  "conda_root": "/home/vieo/anaconda3",
  "conda_envs": {
    "als_sft": "/home/vieo/anaconda3/envs/als_sft",
    "als_vllm": "/home/vieo/anaconda3/envs/als_vllm"
  },
  "available_physical_gpus": [0, 1, 3],
  "default_gpu_plan": {
    "shared_omni": {
      "physical_gpus": [0, 1],
      "tensor_parallel_size": 2,
      "profile": "safe",
      "max_model_len": 4096,
      "max_num_seqs": 1,
      "gpu_memory_utilization": 0.8
    },
    "sft": {
      "physical_gpus": [0, 1],
      "world_size": 2,
      "effective_global_batch": 8
    },
    "evaluation": {
      "physical_gpus": [3],
      "topology": "single",
      "base_port": 8010
    }
  },
  "fixes_included": [
    "all active paths rebound to the double-vieo workspace root",
    "offline conda-pack extraction and conda-unpack helper",
    "scene manifest relocation helper",
    "vLLM 0.11.0 help=all preflight through the environment Python",
    "shared Omni tensor parallel size is configurable rather than fixed at four",
    "evaluation model-key dynamic-scope contamination fix",
    "two-GPU SFT contract accepts the configured world size and preserves effective batch"
  ],
  "scientific_limit": "B0/B1/B2 student model registration remains the original RS-EoT chain; this deployment does not implement Qwen3-Omni as the fine-tuning target."
}
